/**
 * Reversible catalog presentation switches.
 *
 * Keep the visual genre gallery implementation available, but do not render
 * or prepare its artwork while this flag is disabled.
 */
export const ENABLE_VISUAL_GENRE_CATEGORIES = false;

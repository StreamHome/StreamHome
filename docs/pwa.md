# Browser Client

StreamHome `v0.1.0-alpha.1` provides a responsive React browser client.

This release does not include:

- a web app manifest;
- a service worker;
- supported PWA installation;
- offline playback;
- offline media downloads.

Some browsers can create a simple home-screen shortcut to any website. That browser feature does not make the alpha client an offline-capable Progressive Web App.

# Progress Status

## What Works
- [x] **Server Codebase Stabilizations:** Resolved live download progress metrics inside the CLI (implemented raw binary unbuffered process stream configuration to prevent pipeline delays), transcode crashes (`should_cache`), async-sync wait issues on processes (`state.py`), rclone folder structure flattening, metric leakage, database thrashing on active queue ticks, double headers, and converted blocking disk-sync startup calls to async background tasks to prevent 504 gateway timeouts.
- [x] **API Ingestion & Documentation:** External sources can successfully POST new media tasks to the server. Complete structure and samples documented in `@memory-bank\mediaSenderAPI.md`.
- [x] **Clean Ingestion Request Models:** Formulated movie payloads to fully exclude `"season"` and `"episode"` parameters in order to simplify integrations, updating `Dashboard.tsx` form, `test_ingest.py`, and `mediaSenderAPI.md`.
- [x] **Buttery-Smooth Seek Debouncing:** Solved rapid-seek browser decoder lag and audio squeaking by accumulating forward/backward commands and scrubbing ticks, updating the UI slider instantly, and debouncing actual video-element seek operations by 250ms with a temporal event guard.
- [x] **Dynamic Ingestion Preservations and Player Controls:** Pinpointed database paths absolutely to `server/database.db` to prevent duplicate DB instantiations, updated the ingestion worker to save dynamic qualities, subtitles on disk, and audio language details to local `.metadata/metadata.json` configs, and re-engineered `VideoPlayer.tsx` to dynamically parse and render quality options, map specific subtitle formats, and display active audio languages (restricting to exactly sended options with no dummy values).
- [x] **Background Queue:** Downloads are processed asynchronously.
- [x] **TMDB Integration:** Movie and TV show metadata is automatically fetched and mapped.
- [x] **Media Merging:** FFmpeg successfully merges video and audio streams when required.
- [x] **Asset Colocation:** Images are cached securely within their respective media directories.
- [x] **Database Resiliency & Upgrades:** The startup `sync_media_from_disk` hook successfully recovers deleted SQLite databases using local `.metadata` registries and upgrades placeholder assets automatically when credentials are set.
- [x] **Language Customization:** Audio language explicitly passed from the sender is accurately persisted in the database.
- [x] **Dynamic Rename Refactoring:** Added automatic directory and filename renaming when cataloging media, instantly converting placeholder directories (e.g. `Captured Movie...`) to TMDB titles (e.g. `Fight Club_1999_TMDB_550`).
- [x] **Environment Path Bug Fix:** Standardized `.env` loading using path-independent resolving relative to `config.py`, ensuring token credentials load correctly under uvicorn.

- [x] **Dynamic Discover Media Type Filtering:** Restructured discover APIs and frontend calls to filter recommendations by tab (Home vs Movies vs Series). Under Series view, it fetches TV shows (using TMDB `/discover/tv` and TV genre IDs `10759`/`10765`), while movie views query movies, preventing type overlaps.
- [x] **TV Series Local Caching & Nested Episodes:** Fixed series recommendations by aligning the discover folder name predictor with the actual directory structure (`{clean_title}_TMDB_{tmdb_id}`). Fully implemented nested seasons and episodes caching inside both the download queue manager and recommendation system, automatically generating `.metadata` folders, `metadata.json` files, and local images (`poster.jpg`, `backdrop.jpg`, `thumbnail.jpg`) inside each episode folder. Solved Windows-specific file descriptor crashes (`ValueError: too many file descriptors in select()`) by implementing a global image download semaphore, and upgraded logs to show exactly which season/episode is caching. Re-catalog loop (`sync_media_from_disk`) is upgraded to seamlessly parse both styles of directories, strictly verifying physical media presence to prevent placeholder restoration.
- [x] **Dynamic Web Seasons & Episodes Fetching:** Created a backend endpoint (`/api/series/{tmdb_id}/episodes`) and integrated a frontend `useEffect` hook to fetch and map exact seasons, episode numbers, descriptions, and correct durations (lengths) dynamically for all shows, resolving mock placeholders across all detail pages. Solved single-episode database limitations by introducing a hybrid TMDB + SQLite lookup model that displays all available seasons/episodes while enriching downloaded ones with local `video_url` play routes.

- [x] **Global Structured Logging:** Standardized standard python logging using `RotatingFileHandler` writing logs to `server/temp/app.log` (5MB max size, 3 backup files) and console stream, replacing all raw `print()` statements in route/service logic.
- [x] **Database Schema Auto-Migrations:** Added `error_message`, `has_video`, `has_audio`, and `scan_quality` fields to `DownloadTask`, with dynamic startup auto-migrations in `db.py`.
- [x] **Media Ingestion & Stream Scanner:** Enforced 5GB disk checking, ffprobe stream media component probing, and non-blocking REST notifications containing metadata sent to `VIDEO_SENDER_API_URL`. Implemented worker task retry loops with exponential backoff.
- [x] **Startup Integrity Cleanups:** Added database cleanup hooks in `main.py` on startup to reset any dangling tasks in `DOWNLOADING`, `MERGING`, or `MOVING_CLOUD` to `FAILED` with an "Interrupted by server shutdown/restart" error log.
- [x] **Transcode File Caching:** Created segment segment/file caching in `routes/stream.py` under `server/temp/transcode_cache/`. Cached dynamic transcode output is served directly via `FileResponse` for range-seek support.
- [x] **Admin TUI Upgrades (cli.py):** Added setting options to view, set, or clear `VIDEO_SENDER_API_URL` and implemented a fully interactive task scrolling list and detailed inspector pane in the Monitor Queue panel.
- [x] **Complete SVOD Tag Removal:** Removed all occurrences of the term 'SVOD' (case-insensitive) from all user-facing components, metadata descriptions, comments, and logging frameworks across the entire repository.
- [x] **Dynamic Rclone Settings Toggle & Fallback Resilience:** Designed a persistent settings system utilizing `settings.json` (disabled by default) to toggle the storage engine dynamically between `LOCAL` and `CLOUD`. Integrated toggle switches and subpath input fields in Settings view with automatic save-on-blur. Implemented automated fallback handlers that redirect assets from temporary folders to local media catalog directories if the cloud upload pipeline crashes.
- [x] **CLI Storage Settings TUI & Account Management:** Refactored `cli.py` storage engine and remote path prompts into an arrow-selectable sub-menu displaying values dynamically. Renamed User Center to "Account Management", removed the "Unlock User" option, and skipped email prompts for password resets/2FA setup, automatically targeting the registered default admin account.
- [x] **Automated Setup Wizard & Dependency Auto-Installers:** Created `setup.bat` (Windows) and `setup.sh` (Linux/macOS) to install Python 3.11, Node.js 20, local portable **FFmpeg/FFprobe** binaries, and **Rclone**. Implemented an interactive CLI TUI wizard (`cli.py --setup`) featuring dynamic ASCII banner parsing, admin register/2FA setup, verified TMDB token checks against Movie ID `290250` via httpx, and storage setup.
- [x] **Obfuscated HLS segment ingestion bypasses**: Allowed bypass of strict extension validation (`allowed_extensions ALL`, `extension_picky 0`) to scan and ingest streams utilizing disguised `.jpg` segment names.
- [x] **Dynamic Download Speeds & File Sizes**: Enabled real-time calculation and display of downloaded file sizes (MB/GB) and download speeds (Mbps/MBps) in the CLI monitor.
- [x] **CLI Monitor TUI & Process Controls**: Configured `cli.py` active monitor to block Linux stdout keypress pollution, added `Q` and `X` fallback keys, implemented live progress metrics for active tasks, truncated database error messages for failed tasks, optimized the monitor to only redraw on state changes (eliminating screen flicker), and updated `stop.sh` and `start.sh` with port-based process termination fallbacks and virtualenv safety to prevent EADDRINUSE blockages.
- [x] **Proxy Connection Mismatch Fix:** Configured loopback proxying to use `127.0.0.1` explicitly in `web/server.ts` and `web/vite.config.ts`, avoiding Node.js IPv6 name resolution issues.
- [x] **CLI Metrics UUID Type Fix:** Resolved a type-mismatch bug in `cli.py` where SQLModel UUID strings failed to map against JSON dictionary keys, successfully restoring live file size, download speed, and ETA readouts.
- [x] **Audio Extraction Language Fallbacks:** Fixed an issue where undefined (`und`) audio streams extracted by FFprobe were named `und.mp3`, overriding it to forcefully inherit the user's selected original language during ingestion.
- [x] **FFmpeg Audio Header Authentication:** Fixed a critical bug in `ffmpeg.py` where HTTP header tokens were not attached to the audio stream inputs during lossless merges, preventing 403 authorization failures.
- [x] **UTF-8 Byte Stream Recovery:** Resolved live stdout/stderr parsing bugs where multi-byte unicode characters were split and discarded during byte-by-byte extraction, replacing it with string buffers compiled after delimiter detections.
- [x] **Dynamic TV Episode Caching Language:** Patched a metadata generation bug in `tmdb.py` that forced TV series episodes to `"en"` original language fallbacks, successfully mapping parent show properties to metadata files.
- [x] **Backup Worker Retries Optimization:** Reduced database lock delay check sleep times from 1 hour to 5 minutes to prevent deferred daily backups from slipping arbitrarily.
- [x] **Process Registry Typing Alignment:** Standardized type references to `Union[asyncio.subprocess.Process, subprocess.Popen]` inside thread-tracking models.
- [x] **MovieResponse Validation Patch:** Added resilient getter logic to SQLModel's `languages` properties in `models.py` to auto-wrap plain strings into lists, bypassing `ValidationError` exceptions for corrupted ingestion states.
- [x] **TV Episode Active URL Playback Fix:** Updated `VideoPlayer.tsx` to dynamically override the parent `movie` interface with active episode configurations for `videoUrl`, `quality`, `languages`, and `subtitles`. Resolves infinite buffering bugs caused by passing empty video paths to the transcoder during TV show playback.
- [x] **Robust PWA Offline Capabilities:** Replaced the fragile manual `sw.js` with `vite-plugin-pwa` and Workbox. The UI is now fully injected into the cache at build-time for 100% offline availability. Configured `StaleWhileRevalidate` for dashboard API routes, while completely removing `/media/` from Service Worker interception to prevent `CacheFirst` multi-gigabyte browser freezing. Moved `vite-plugin-pwa` to regular dependencies to prevent runtime crashes.
- [x] **Video Player & Server Stability Improvements:** Fixed `NotSupportedError` crashes caused by empty video tags by dynamically parsing `movie.videoUrl` fallbacks. Prevented `AbortError` stack trace flooding in the console during rapid play/pause actions by adding catch blocks to HTML5 media promises. Intercepted `401 Unauthorized` API fetch errors in the dashboard to immediately log users out instead of hanging. Disabled Vite HMR development loops on production builds by checking `.cjs` launch targets. Fixed infinite buffering when playing TV Series directly from billboards by safely intercepting the play request and routing the user to the Details Modal to pick an episode. Forced the UI scrubber to fall back to database metadata duration when the browser incorrectly parses chunked MP4 video durations as ~10 seconds.

- [x] **Data Saver & Adaptive Bitrate Optimization:** Added an `"Auto"` quality streaming engine that actively monitors the browser's buffer health and dynamically shifts resolutions down (1080p -> 720p -> 480p -> 360p -> 240p) if the internet speed drops or buffering occurs. Heavily optimized backend transcoder via strict `-maxrate` limits, minimizing data usage for cellular watchers (~110MB/hr on 240p).
- [x] **TIDB Skip Intro & Credits Auto-finish Integration:** Decoupled server-side fetching of `api.theintrodb.org` to comply with third-party service terms. Media Sender API (client extension) now fetches skip markers securely on the user's browser and transmits them inside the `/api/add-movie` ingestion payload. Backend effortlessly persists these markers in `database.db` and `.metadata/metadata.json`, fueling desktop and mobile web player styled skip overlays and S-key auto-exits.
- [x] **Background HEVC Compression Engine (Library Optimization):** Implemented a 24/7 background worker loop that silenty compresses local videos to `libx265` (-threads 2) to reduce disk space by 50%. Features an active user kill-switch that instantly aborts transcoding if someone logs in or starts playing, and a hardware protection auto-guard that disables the worker completely if the server has fewer than 4 CPU cores. Integrated full Admin controls directly in the React frontend settings.
- [x] **Placeholder Metadata Recovery Integrity:** Fixed a critical `sync_media_from_disk` crash where placeholder media files failed the strict physical file check, restoring graceful metadata recovery without failing database cataloging.

- [x] **Web Phase 0 (Scaffolding):** Bootstrapped the new React + Vite + Tailwind v4 frontend inside `web/`. Initialized `package.json`, configured `tsconfig` for strict TS, `vite.config.ts` for backend API proxying, and built a minimal `App.tsx` router entrypoint with global CSS resets. Verified compilation and bundle outputs.
- [x] **Web Phase 1 (TypeScript Definitions):** Created strict TS typings for all backend JSON models (e.g. `Movie`, `Episode`, `Profile`, `DownloadTask`, `SystemSettings`) inside `web/src/types/api.ts` utilizing `camelCase` field mappings. Also established `ThemeId` and `ThemeConfig` models in `web/src/types/theme.ts`.
- [x] **Web Phase 2 (API Client Layer):** Created a centralized `apiFetch` wrapper inside `web/src/api/client.ts` handling JWT injection and automatic 401 unauthenticated redirect logic. Established domain-specific client modules (`auth.ts`, `profiles.ts`, `movies.ts`, `playback.ts`, `watchlist.ts`, `system.ts`, `backup.ts`, `update.ts`) matching backend routes exactly.
- [x] **Web Phase 3 (State Management):** Configured lightweight global state using Zustand. Created `authStore.ts` (JWT/email persistence and logout routing), `profileStore.ts` (active profile persistence and admin evaluation), and `themeStore.ts` (dynamic `data-theme` syncing on `document.documentElement`).
- [x] **Web Phase 4 (Theme System):** Built the complete 4-tier design system architecture (Ember, Aurora, Cinema, Gemini). Created individual CSS payload files with token mappings (colors, glows, geometries, typography, animations), an aggregator index mapping metadata, and React-based interactive canvas/WebGL-style dynamic background components (Drifting Embers, Liquid Mesh, Starfield) for each theme.
- [x] **Web Phase 5 (Shared UI Components):** Engineered the core design system components mapping exactly to the CSS variables. Built utility formatters (`formatDuration`, `formatFileSize`, `formatTimeAgo`) and `cn` for `clsx` merging. Created the foundational `<GlassPane />` with dynamic spotlight tracking, a multi-variant `<Button />`, animated `<Input />` and custom framer-motion `<Dropdown />` selects, alongside progress indicators (`ProgressBar`, `ProgressRing`), `<Modal />`, and `<Chip />`.
- [x] **Web Phase 6 (Layout & Navigation Components):** Programmed the 4 distinct layout wrappers mapped to the themes. Created the `EmberNavbar` with scroll-dependent backdrop blur scaling, the floating `AuroraNavbar` glass pill, the classic gradient-fading `CinemaNavbar`, and the completely unique framer-motion collapsible side-drawer for `GeminiNavbar`.
- [x] **Web Phase 7 (Page Implementations):** Executed the massive routing and view layer. Built `App.tsx` with strict auth/profile/admin guards. Created the secure 2FA `LoginPage.tsx` and the dynamic `ProfileSelectPage.tsx` with cross-fading ambient themes. Implemented `DashboardRouter.tsx` mapping to `EmberDashboard.tsx` and all internal tabs (`EmberHome`, `EmberMovies`, `EmberSeries`, `EmberDownloads`) alongside `MediaCard` and `MediaRow`. Finished the phase with `DetailsRouter.tsx` overlays and the highly complex `PlayerPage.tsx` video wrapper with `EmberPlayer.tsx` custom controls.

- [x] **Web Phase 8 (Admin Center):** Built the complete `AdminGate.tsx` featuring an animated lock-screen overlay with password/TOTP verification and a 24-hour TTL session localStorage mechanism. Constructed the highly advanced `AdminCenter.tsx` layout structure containing the sidebar and 6 specialized panels (`UsersPanel`, `StoragePanel`, `DownloadsPanel`, `BackupPanel`, `UpdatePanel`, `HEVCPanel`). Implemented the global `SudoModal.tsx` requiring inline re-authentication prior to sensitive actions like user deletion, storage engine switches, or database restoration.
- [x] **Web Phase 9 (Routing & Integration):** Successfully wired all endpoints into `App.tsx` utilizing strict contextual router guards: `AuthGuard.tsx` (blocks `/profiles`), `ProfileGuard.tsx` (blocks the core Dashboard & Player), and `AdminGuard.tsx` (blocks `/admin/*`).
- [x] **Web Phase 10 (Verification):** Conducted rigorous TS type alignments replacing mock snake_case API references with backend-accurate `camelCase` properties, fixing over 45 TypeScript compilation errors automatically with a custom AST node script. Verified 100% clean passes on `npm run lint` and `npm run build`.
- [x] **Web Phase 11 (UI Bug Fixes):** Fixed z-index and click interception issues in EmberHome caused by invisible absolute gradients without pointer-events. Purged overlapping ambient background canvases that duplicated rendering loops. Restored mock fallback images (poster and backdrop) globally by linking them straight into the Vite public asset directory for resilient localhost mapping.

## What's Left to Build
- [ ] **Rclone Upload Progress Tracking:** Parse rclone progress output to display upload percentage, speed, and ETA in the web dashboard interface instead of running silent background tasks.
- [ ] **Automatic Subtitle Auto-Conversion (VTT to SRT):** Automatically convert incoming WebVTT (.vtt) subtitles into SubRip (.srt) format during ingestion using a lightweight parser to maximize player compatibility.

## Current Status
The backend server and frontend streaming UI are highly stable, featuring secure user-level log-in gates, TOTP 2FA setup, responsive layouts, cinematic transitions and animations, robust profile settings, database resilience, metadata discovery, structured logging, stream caching, and an interactive CLI TUI. The core requirements for the self-hosted media platform are fully complete, and UI edge-cases have been patched. The project is now transitioning into the final stretch of backend features (Rclone tracking and Subtitle conversions).
 
 